@model PSV.Models.DTOs.OrderControl

<main>
    <a class="action-button" href="@Url.Action("Menu", "Wrapping")">
        <span class="material-symbols-outlined">
            arrow_back
        </span>
    </a>
    <h2 class="page-title">Stanowisko cięcia - zlecenie nr. @Model.OrderNumber</h2>
    <div class="decision-buttons-space">
        <h2>Czy chcesz dodać komentarz?</h2>
        <div class="decision-buttons">
            <a class="control-button-start" href="@Url.Action("Menu", "Wrapping")">Nie</a>
            <a class="control-button-end" href="@Url.Action("CommentsForm", "Wrapping", new {Model.Id})">Tak</a>
        </div>
    </div>
</main>